# 0.4.14.88 任意テスト配布 (#386)

0.4.14.87の配布ブランチを基点に、mainの #385 を取り込む。

- #385: 強制LNの初期値をONにし、設定で外すopt-out方式へ変更。#345以降の版が保存した
  `bmsirForceLn=false` は一回だけONへ寄せ（`bmsirForceLnDefaultApplied`）、以後の
  起動前コンフィグでの変更を保持する。Arenaの固定・退室時復元、成績DB分離、リプレイ、
  IR送信契約は変更しない。

基点 `d079b1a8`、取り込み元 `51e1c954`（mainのマージコミットを `-m 1` で取り込み、競合なし）。

本体と対応ソースZIPを両OSに配布する。配布スキンの変更はない。
plugin 0.0.75とlauncher 0.2.29、ネイティブ音声基盤は継続する。paired plugin
BMS-Mania/bms-ir-plugin#51（ランキングXMLのlntypeフィルタ）は現行サーバー応答では挙動差が
ないため、この本体テスト配布には含めない。
更新は任意。サーバーの既存ゲート追加・必要な再読込・公開検証は BMS-Mania/IR 側の配布Issue。

検証は全coreテスト、PR CI、確定コミットの両OSビルド、ソースZIPと署名履歴の整合確認。
実機は #386 で、初回起動後に起動前コンフィグの「強制LN」がONになっていること、OFFへ変更した
後に再起動してもOFFが保持されること、CN/HCN指定譜面がLNとしてプレイされ強制LN用の成績プールが
使われること、Arena退室後に設定が復元されることを確認する。
