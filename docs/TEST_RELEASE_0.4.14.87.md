# 0.4.14.87 任意テスト配布 (#381)

0.4.14.86の配布ブランチを基点に、2026-09-11時点のmain追加変更をすべて統合する。

- #373: LR2固定HS増減幅、強制LNランキング設定共有、オーバーレイ操作終了後の入力解放。
- #377: REGUL SPEED (302) とoraja CONSTANT (400) の表示分離。
- #378: 単曲のDAN GAUGE / INITIAL GAUGEとリプレイ対応。
- #380: ASSISTキーでREGUL SPEEDを切り替える動作。

基点 `0afe02d8`、取り込み元 `52f7895e` / `f9cb7561` / `31396cda` / `122a6e66`。
mainと配布ブランチは履歴・配布スキンが異なるため、既存配布内容を保って取り込む。
文書競合を統合し、削除済みの全スキン用テストを復元する代わりに、配布スキンの
CONSTANT ref/actが400であることを実ローダーで確認するテストを加えた。
ASSISTキーの競合は最新mainのtoggleRegulSpeed呼び出しへ統一し、その回帰テストを収録する。

本体と対応ソースZIPを両OSに配布し、変更した `skin/default/select.json` も署名付き
差分に含める。plugin 0.0.75とlauncher 0.2.29、ネイティブ音声基盤は継続する。
更新は任意。サーバーの既存ゲート追加・必要な再読込・公開検証は BMS-Mania/IR#1244。

検証は全coreテスト、macOS本体ビルド、リリースhelperと機能保持チェック、PR CI、
確定コミットの両OSビルド、ソースZIPと署名履歴の整合確認。
実機は #372 で、HSリトライ、LN/CN/HCN切替、編集後テンキー、単曲段位ゲージと
リプレイ、REGUL SPEED/CONSTANTの独立動作を確認する。
ランキングの報告された表示不具合は実機未再現であり、自動検証だけで解消と断定しない。
